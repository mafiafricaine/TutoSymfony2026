{% extends 'base.html.twig' %}

{% block title %}#{{ recipe.id ~" "~ recipe.slug }}{% endblock %}

{% block body %}
    <div class = "row">
        <div class= "col-md-6 mx-auto">
            <h1>{{ recipe.title | title }}</h1>
    {#Attention, le form est la fonction fourni par symfony, et monForm c'est le nom qui vient de la méthode edit dans RecipController #}
            {{ form(monForm) }}
        </div>
    </div>
    {# Ici c'est une des manières de modifier l'agencement du formulaire #}
    {# {{ form_start(monForm) }}
        <div class = "d-flex">
            {{ form_row(monForm.title) }}
            {{ form_row(monForm.duration) }}
        </div>
    {{ form_end(monForm) }} #}

{% endblock %}